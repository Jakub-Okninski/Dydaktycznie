@model IEnumerable<dynamic>

@{
    ViewData["Title"] = "Quiz Statistics";
}

<h1>Statystyki Quizów</h1>

<table class="table">
    <thead>
        <tr>
            <th>Autor</th>
            <th>Ilość Quizów</th>
            <th>Łączna Ilość Pytań</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.AuthorName</td>
                <td>@item.QuizCount</td>
                <td>@item.TotalQuestions</td>
            </tr>
        }
    </tbody>
</table>
